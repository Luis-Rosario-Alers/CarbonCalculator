[tool.isort]
profile = "black"
line_length = 88

[tool.black]
line-length = 88

[tool.pytest]
pythonpath = [".", "src"]

[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[tool.mypy]
python_version = "3.8"
pythonpath = [".", "src"]
